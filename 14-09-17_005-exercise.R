## learning about ggplot2

install.packages("ggplot2", dependencies = T)
install.packages("tidyr", dependencies = T)

library(ggplot2) #this needs to be at the top of a script for a ggplot script to run
#ggplot2 ONLY works with data.frames
#ggplot2 construct the figure as an Robject, so you need to print it to see it

